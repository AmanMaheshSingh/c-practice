#include<stdio.h>
#include<string.h>

int main()
{
    char str[] = "mai aman hu yak greatest codder";
    for(int i = 0 ; i < strlen(str);i++){
        str[i]= str[i] + 1; //nbj!bnbo!iv!zbl!hsfbuftu!dpeefs
    } 
    printf("%s",str);
    return 0;
}